#include "stdio.h"
#include <stdlib.h>

char *secret = "pqrstuv";

int main(){
	
}

char* addNum(char* s, int n){
	int i;
	for(i = 0;s[i] != '\0';i++)
		;
	int j;
	int temp = n;
	for(j = 0;temp>0;j++)
		temp /= 10;

	char *fullString = (char *)malloc(i+j+1);
	int index = 0;
	for(i = 0;s[i] != '\0';i++)
		fullString[index++] = s[i];

	for(i = 0;n>0;i++){
		fullString[index++] = n/10;
	}
}
